<div id="index">

    <h1>List of manufacturers</h1>
    <%= link_to("New Manufacturer", new_manufacturer_path, class: "standard-button") %>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Location</th>
                <th class="link-column"></th>
                <th class="link-column"></th>
                <th class="link-column"></th>
            </tr>
        </thead>
        <tbody>

            <% @manufacturers.each do |manufacturer| %>
                <tr>
                    <td class="center"><%= manufacturer["id"] %></td>
                    <td><%= manufacturer["name"] %></td>
                    <td class="center"><%= manufacturer["location"] %></td>
                    <td><%= link_to("Show", manufacturer_path(manufacturer["id"])) %></td>
                    <td><%= link_to("Edit", edit_manufacturer_path(manufacturer["id"])) %></td>
                    <td><%= link_to("Delete", manufacturer_path(manufacturer["id"]), method: :delete, data: { confirm: 'Are you sure?' }) %></td>
                </tr>
            <% end %>

        </tbody>

    </table>

</div>